import React from "react";
import ReactDOM from "react-dom/client";
import GaleriKu from "../Galery_app.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GaleriKu />
  </React.StrictMode>
);
